import { Util, Theme, Icons, Logic } from "../../def.slint";
import {
    ExampleComponent,
    ColorPicker,
    ColorPickerPopup,
} from "../../../base/widgets.slint";

export component ColorPickerExample inherits Flickable {
    viewport-height: vbox.preferred-height;
    viewport-width: vbox.preferred-width;
    interactive: false; // disable dragging

    vbox := VerticalLayout {
        spacing: Theme.spacing * 20;
        padding-top: Theme.padding * 5;
        padding-bottom: Theme.padding * 5;
        alignment: LayoutAlignment.center;

        HorizontalLayout {
            width: root.width;
            alignment: LayoutAlignment.center;

            ExampleComponent {
                width: Math.min(1000px, root.width * 0.8);

                VerticalLayout {
                    spacing: Theme.spacing * 10;

                    HorizontalLayout {
                        alignment: center;

                        ColorPicker {
                            canceled => {
                            }
                            confirmed(color) => {
                                debug(color);
                            }
                        }
                    }

                    HorizontalLayout {
                        alignment: center;
                        Rectangle {
                            background: gray;
                            width: txt.preferred-width + Theme.padding * 4;
                            height: txt.preferred-height + Theme.padding * 2;

                            txt := Text {
                                text: "Show ColorPicker";
                            }

                            TouchArea {
                                clicked => {
                                    cp.show();
                                }
                            }
                        }
                    }
                }

                code: "ColorPicker {\n    canceled => {\n    }\n    confirmed(color) => {\n        debug(color);\n    }\n}\n\n ColorPickerPopup {\n    x: (root.width - self.width) / 2;\n    y: (root.height - self.height) / 2;\n}";
            }
        }
    }

    cp := ColorPickerPopup {
        x: (root.width - self.width) / 2;
        y: (root.height - self.height) / 2;
    }
}
