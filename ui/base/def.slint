export enum LoadingStatus {
    Loading,
    Fail,
    Success,
}

export enum PrioritizationFeeStatus {
    Slow,
    Normal,
    Fast,
}

export enum ToastStatus {
    Success,
    Info,
    Warning,
    Danger,
    None,
}

